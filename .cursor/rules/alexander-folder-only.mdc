---
description: Alexander's work stays in alexander/ folder; never modify team files at repo root
alwaysApply: true
---

# Alexander's work: alleen in alexander/

- **Wijzigingen voor Alexander** moeten altijd in de map **`alexander/`** gebeuren, niet in de root van de repo.
- **Nooit** bestanden buiten `alexander/` aanpassen tenzij de gebruiker dit **expliciet** vraagt (teamcode blijft onaangeroerd).
- Nieuwe bestanden voor dit project: aanmaken onder **`alexander/`** (bijv. `alexander/scripts/`, `alexander/epc-architecture/`).
- Bij pushen naar GitHub: ervoor zorgen dat de **`alexander/`** map up-to-date is (bijv. root → alexander/ kopiëren en dan alexander/ committen en pushen), niet de root overschrijven met teamcode.
- **Altijd naar twee plekken pushen** (beide moeten gebeuren):
  1. **Vercel:** `git push vercel main`. Repo = ProjectvakAKA/projectvak-vercel. In Vercel staat Root Directory op **alexander/epc-architecture**. Eerst sync: root `epc-architecture/` → `alexander/epc-architecture/` (rsync), dan commit, dan push vercel.
  2. **Arthur (origin):** `git push origin main`. Repo = Arthur-Code-123/HIRBProjectvak. Onze commits wijzigen **alleen bestanden in de map alexander/**; daardoor komt er in Arthur’s repo alleen iets bij in **jouw mapje (alexander/)**, niet in de root of andere teammappen. Gewone push, geen subtree.
